# 第九轮：复用执行中已完成的图表 K 线

继续基于 `172ae4dd` 与前八轮未提交修改；未提交、合并或部署。第七轮 JSON 片段复用仍关闭。

## 改动

双时钟策略执行已经逐条构建完整 K 线。原服务结束后，为图表缓存再次调用 `derive_complete_trade_bars()` 扫描整个成交序列并构建相同的 OHLCV。

现在在新运行、纯 TRADES 输入时，将执行过程中已完成 K 线转换成原图表行格式并收集，结束后直接保存；省去第三次 K 线构建（主运行、成本分析仍照常构建）。图表行使用与原实现相同的字段和数值转换，末尾未完成 K 线不加入图表。

已有恢复检查点或带辅助事件的输入继续走原重建路径，避免恢复图表缺失前半段，也保留现有辅助事件路径的行为。未修改 SDK、撮合、成本分析、checkpoint 格式或图表格式。

候选默认关闭：`BACKTEST_REUSE_DUAL_CHART_BARS_ENABLED=0`，显式设为 `1` 才启用复用；原因是长区间没有稳定收益。运行中保留图表行列表，有一定额外在途内存；没有同时保留完整 MarketEvent K 线列表，最终原有 50,000 根图表上限和报告字节上限仍保留。此实现没有做跨运行缓存，也不会向策略暴露未来 K 线。

## 验证

- 1、25、501 条行情 × 1m/5m 周期：从同一排队数据库复制运行，比较完整 result/report 与整个已存图表缓存，包括 JSON、哈希、数量和时间字段。
- 用函数调用计数确认新运行确实不再调用末尾重建函数。
- 在第 200 条行情保存检查点后中断，恢复完成：确认使用原重建函数，并与不中断运行比较完整结果及全图表内容、哈希、数量。
- 扩展回归继续覆盖前序优化、账户、资金费、撮合、恢复及分块校验。

## 配对基准

保持前序优化开启、第七轮关闭，仅交替本轮开关；保留完整成本敏感性分析和周期检查点。使用合成聚合成交、SMA 3/5、每根 K 线 10 条成交，每 1,000 条行情保存检查点，每组 3 对且第二对先开启优化。

```powershell
$env:PYTHONPATH='backend;packages/candlescope-plugin-sdk/src;packages/candlescope-backtest-sdk/src'
python -m scripts.benchmark_trade_strategy --round9 --events 100000 --pairs 3 --output docs/performance-evidence-20260919/round9-dual-100k.json
python -m scripts.benchmark_trade_strategy --round9 --events 300000 --pairs 3 --output docs/performance-evidence-20260919/round9-dual-300k.json
```

计时包含 worker、策略、撮合、完整成本分析、检查点及报告/图表持久化；不含导入、HTTP 或浏览器。基准与测试串行运行。结果仅适用于这些本地合成行情样本，不代表真实交易所全量数据或生产性能。

## 配对结果与启用决定

| 行情数 | 配对 | 关闭 | 开启 | 耗时降幅（负值表示变慢） |
|---|---|---:|---:|---:|
| 100k | 1 | 4.631 s | 4.417 s | 4.62% |
| 100k | 2 | 4.834 s | 4.569 s | 5.48% |
| 100k | 3 | 4.809 s | 4.437 s | 7.73% |
| 300k | 1 | 17.842 s | 18.737 s | -5.01% |
| 300k | 2 | 16.677 s | 17.146 s | -2.82% |
| 300k | 3 | 15.836 s | 14.367 s | 9.28% |

中位耗时：

- 100k: 4.809 -> 4.437 s; median elapsed reduction 7.73%
- 300k: 16.677 -> 17.146 s; median elapsed reduction -2.82%

六对完整 result/report 一致。100k 三对均变快，300k 两对变慢、一对变快，未达到跨负载稳定收益条件，因此不替换默认路径。此结果不能确定是额外在途数据、分配/GC 开销还是整机波动导致，需要独立内存与分配分析；不把任一原因当作已证实。第八轮记录编码优化保持开启。

最终回归在候选显式开启时通过 **349 项**，无失败、错误或跳过；4 条已有 FastAPI 弃用警告。专项测试同时覆盖开关两侧及恢复回退。Ruff 和 `git diff --check` 通过。证据：`performance-evidence-20260919/round9-regression.xml`、`round9-validation.json`。
