# F12a 副图标签跨窗格重叠

原因：副图标签只同步顶部位置，不限制高度；多行指标数值、来源和倒计时在矮窗格中溢出到后续副图。

修复：定位同步时传入窗格可用高度，标签裁切在所属窗格内。低于 100 CSS 像素的副图采用单行标题和数值，收起重复的线名、市场数据来源及倒计时等次要文字。保留完整辅助功能说明，并增加原生 title 悬停提示。恢复较高窗格时仍使用完整标签布局。没有改变图表数据、指标公式或窗格比例。

验证：相关 ESLint、TypeScript、Production build、git diff --check 通过。电脑操控生产构建，检查双图各含资金费率、持仓量、MACD、RSI，分别关闭及打开底部策略面板；矮窗格标签不再跨越到下一窗格，数值仍可读。证据 output/user-audit-20260907/evidence/fix-F12a-before.png、fix-F12a-tall.png、fix-F12a-short.png（另附同名辅助功能文本）。构建与类型检查日志为 output/user-audit-20260907/fix-F12a-*.log。

边界：此提交仅解决副图标签溢出；F12 的主图 OHLC 紧凑布局及最小可读窗格高度仍需分别处理。悬停提示由浏览器原生 title 提供，未以截图验证弹出时机。
