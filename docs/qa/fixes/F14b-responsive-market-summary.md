# F14b：次要行情摘要挤压顶部功能入口

对应原始审计 F14。F14a 已处理合约代码换行，本次解决标记价、指数价和基差始终占据固定宽度、压缩交易对和策略入口的问题。

## 修改

行情页在 CSS 视口宽度不超过 1600px 时，将三项合约行情折叠到“合约市场摘要”。点击或键盘展开可读取原有数值，Escape 或焦点移出时收起；超过阈值继续直接展示。两种布局共用同一份行情内容，保留连接状态样式和基点值。沿用现有翻译与主题颜色，不改变数据请求。

## 验证

- `npm run build` 成功，生产预览 15173、后端 18080 无热重载。
- TypeScript、针对性 ESLint、`git diff --check` 通过。
- `marketPageFrame.test.tsx` 和 `shellI18n.test.tsx` 共 16 项通过；这些测试验证页面结构和国际化，响应式布局由下面的 GUI 测试验证。
- Chrome 电脑操控：原 994×768 截图窗口、100% 页面缩放下，BTC-USDT-SWAP 和图表策略状态完整显示，顶部未增高；摘要可以展开，三项数值可见。
- Escape 关闭和 Tab 移出焦点关闭均通过。宽布局复测采用浏览器缩放改变 CSS 视口，确认重新直接显示行情值；完成后恢复 100%。
- 临界宽度仍允许代码单行省略并保留完整辅助名称，未承诺任意语言、插件数量和超窄窗口下均完整显示。

证据：`output/user-audit-20260907/evidence/F14b-{compact,expanded,midwidth,wide,final-compact,final-expanded,blur-closed}.*`。构建、类型和测试日志为该输出目录下 `F14b-*.log`。

2026-09-08 恢复测试时确认旧预览及后端已停止，随后重新启动生产预览与无 reload 后端，完成最终展开和焦点复测。服务重启期间的连接错误不计入本修复效果。浏览器生产构建验证不代表原生安装包验证；未修改回放或独立研究页的顶栏布局。
