# F17f：顶部功能入口图标风格混用

对应审计 F17。顶部设置、指标和警报原来直接显示平台 Emoji，与细线侧栏图标不一致。

## 修改

三个入口改用 18px、1.8 线宽、currentColor 的 SVG，与现有侧栏一致。设置复用齿轮，指标复用柱形图，新建警报铃铛图形；按钮统一复用现有悬停样式。指标数量角标保留，指标和警报增加可访问名称及展开状态，装饰图标不参与读屏名称。

## 验证

- `npm run typecheck`、针对性 ESLint、`git diff --check` 通过。
- `npm run build` 成功，使用 127.0.0.1:15173 生产构建进行电脑操控复测。
- 深色下三个图标清晰、尺寸一致；指标角标仍为 5，点击进入指标列表。警报入口打开警报中心；设置入口打开设置。
- 切换亮色并关闭侧栏，确认图标使用主题颜色而非彩色 Emoji；最后恢复深色。
- 截图及 AX 文本保存为 `output/user-audit-20260907/evidence/F17f-{dark,indicators,alerts,light,restored}.*`；构建与类型日志为 `F17f-*.log`。

本次为可逆的外观与可访问性调整，使用发行构建实际操作验证，未新增只检查图标路径的镜像单元测试。范围仅为行情页顶部三个入口；指标列表分类、设置页插图及回放术语等其他 F17 观察未在此提交中关闭。浏览器生产构建验证不代表原生安装包验证。
