# F17a 关于页快捷键缺少实际键位

复现：生产构建打开设置→关于，快捷键区域仅显示设置、指标、周期名称与图标，没有键位。当前代码也没有为这三个入口绑定对应快捷键。

修复：替换为已实现的五条操作说明：切换交易对搜索、在非输入状态打开搜索、关闭搜索、工作区布局撤销、工作区布局重做。每行以 kbd 显示键位。根据平台显示 Mac 的 ⌘ 或其他桌面平台的 Ctrl；布局历史明确限定“工作区面板内”。九种语言补齐说明，长说明允许换行，键位不折行。本次未新增键盘行为。

验证：

- 生产构建电脑操控复现旧页面，更新后五条快捷键同屏完整显示，没有裁切。截图及 AX：`output/user-audit-20260907/evidence/fix-F17a-before`、`fix-F17a-shortcuts`。
- Mac GUI 实测 ⌘K 打开搜索、Esc 关闭、斜杠打开搜索、再次 ⌘K 关闭搜索均成功。证据 `fix-F17a-command-k`、`fix-F17a-slash`。
- 3 项已有快捷键测试通过，覆盖 Mac/Windows 工作区撤销重做、输入框/不可撤销状态不处理、搜索 Escape 事件隔离。没有为纯说明文本编写镜像测试。
- TypeScript、相关 ESLint、生产构建、git diff --check 通过。日志：`output/user-audit-20260907/F17a-*.log`。

边界：本轮未在 Windows GUI 或其他语言界面复验；工作区撤销重做采用现有行为测试。本次仅解决 F17 的关于页键位缺失，其余技术参数名称、英文术语与图标风格仍待处理。搜索入口仍显示 Ctrl 的旧提示属于 F22，未混入本提交。
