# F31：ATR 初始真实波幅平均的实时预览缺失

日期：2026-09-08。

ATR 的初始平均在当前 K 线满足周期时已能计算，但实时路径要求 _atr 已存在，导致首个有效值只在收盘后显示。

修复：在初始边界用累计真实波幅加当前真实波幅产生临时平均；保留尚未满足周期的空值和后续 Wilder 平滑，不推进确认数据。

独立手算用首根高低差 3、后续跳空真实波幅 6，覆盖周期 1/2/14/100、重复预览、确认收盘及下一根递推。修复前 4 组全部失败，修复后相关指标/API 回归 106 项通过。Mac arm64 原生包重新生成成功，production 前端未变。

证据：output/user-audit-20260908/atr-preview-before.log、atr-preview-tests.log、atr-preview-package.log。没有依赖 GUI 等待实时预热边界。
