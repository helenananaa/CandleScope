# F20d：进入训练后选品侧栏仍暴露内部术语

原 F17/F20 的 Run、MarketTrack 文案问题在创建表单已有修正，但进入回放后的选品侧栏仍显示“搜索当前 Run 可用商品”“成为 MarketTrack”，顶部还直接列出 NONE/WARM/FULL。该普通操作入口与创建训练说明使用不同概念。

简体、繁体中文将搜索和已添加提示统一为“本次训练”，说明一条训练可包含多个商品，添加时校验历史数据并对齐当前训练时间。紧凑标题改为“归档快照 · 分级订阅”。保留时间对齐条件，不改订阅枚举、校验或账户规则。

验证：现有回放工作区 30 项测试、TypeScript、production build、diff check 通过。电脑操控发行预览中的 QA-F20c 存档，截图确认标题及说明可见；AX 标签与禁用的已添加商品提示正确。搜索 BTC 仍保留已加入商品，清空搜索后恢复；训练保持暂停。

证据为 `output/user-audit-20260907/evidence/F20d-{before,after,search}.{png,txt}` 与 `F20d-{tests,typecheck,build}.log`。

本次是中文普通选品流程文案修正，未改其他语言、技术详情或存档中用户自定义列表名称。不会将该局部文案修复宣称为完整回放功能或全语言验收；浏览器 production 预览不是 Windows 原生包验证。
