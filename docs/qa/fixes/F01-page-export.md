# F01 页面可见区导出

问题：实时页面的异步 DOM 截图与绘图场景一致性校验冲突，PNG/WebP 多次失败。

修复：在同一同步执行阶段复制 DOM、计算样式、表单值及 canvas 位图；固定捕获源后才校验、恢复绘图 lease，再异步栅格化。冻结副本置于不可交互的屏幕外容器中，让 Chromium 能读取正确样式，完成或失败都移除容器。没有关闭过期场景校验，普通图表/主窗格仍走原有同步 canvas 路径。

验证（2026-09-07，Mac Chrome，production build / preview）：

- 导出和绘图 barrier/lifecycle 测试 41 项通过，含新增快照不受后续行情像素、样式、表单更新影响的回归测试。
- TypeScript 检查、修改文件 ESLint、production build 通过。
- OKX 永续双图、五指标场景，页面 PNG/WebP 2x 实际保存；打开 PNG 确认布局、图形、文字均保留。
- 有绘图的页面捕获、隐藏绘图预览及恢复路径通过；新增绘图后首次捕获曾被现有过期保护拒绝，稳定后重试成功。没有将该保护移除或保证任何场景绝不需要重试。
- 首版离屏副本未挂载导致样式丢失，已在文件视觉复核中发现并修正，不作为通过版本。

本地证据：`output/user-audit-20260907/evidence/fix-F01-*`；构建、测试、类型检查日志在同级目录。此提交仅处理 F01；导出浮层窄布局与默认元数据等其他问题不包含在内。
