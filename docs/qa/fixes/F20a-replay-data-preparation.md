# F20a：无覆盖训练的数据准备入口

对应原始审计 F20。本次只处理行情页回放在 BAR 历史覆盖不足时缺少下一步入口的问题。

## 修改

- 无符合预热和前向时长要求的连续历史时，新建训练显示“准备历史数据”。
- 在原弹窗中打开数据工作台，手动下载表单带入当前交易所、市场和商品。
- 关闭按钮或 Escape 返回训练表单，保留草稿并重新查询覆盖目录。视图切换时恢复弹窗焦点，保证键盘返回可用。
- 复用现有按钮样式，补齐九种语言。保留覆盖校验，不自动下载或创建训练。

## 验证

- `npm run build` 成功；使用 127.0.0.1:15173 的生产构建进行 Chrome 电脑操控测试，后端无 reload。
- 类型检查、针对性 ESLint、`git diff --check` 通过。
- 回放 Hub 与 launcher 测试共 32 项通过，包括重新打开保留草稿和刷新目录；新增渲染断言覆盖无 BAR 覆盖、有覆盖、AGG_TRADE 和未提供入口回调的边界。
- 真实库存下新建训练显示 0 个覆盖商品。将名称改为 `QA-F20a 保留草稿`，打开数据工作台，确认下载表单预填 OKX / 永续合约 / BTC-USDT-SWAP。
- 分别用 Escape 和关闭按钮返回，名称保留，准备入口继续可用。后台目录请求重新返回 200，参数为 warmup_bars=200、horizon_ms=86400000、blind_mode=false、source_kind=BAR。
- 实测过程中修正切换视图后焦点落回页面、Escape 无响应的问题，并复测通过。

证据：`output/user-audit-20260907/evidence/F20a-{no-coverage,workbench,escape-return,close-return,final}.png` 及同名 AX 文本；测试与构建日志为该输出目录下 `F20a-*.log`。

## 边界

- 未实际下载历史、创建训练或验证下载后完整回放流程。本次验证的是数据准备入口和返回衔接。
- 此入口位于行情页启动的回放弹窗；独立回放页、AGG_TRADE 历史准备不在本次范围。
- 原生发行包的平台限制仍沿用原审计说明；本次没有将浏览器生产构建验证写成原生安装包验证。
- F20 其余术语和训练流程观察未在此提交中关闭。

## 后续闭环修正（2026-09-08）

真实下载证明实时库存并不会自动成为回放归档；本记录的入口验证不代表数据已接通。归档转换及返回后的能力缓存修正在 [F20c](F20c-manual-history-replay-archive.md)，并完成创建、选品和单步推进验证。
