# F09b 编辑器工具栏逐字换行

原因：标题、名称、操作区强制挤在单行 flex 中，按钮允许收缩，400 CSS px 左右的侧栏把运行/保存中文挤成竖排。

修复：工具栏允许标题组和操作组整体换行；操作按钮不收缩且文字不换行，长指标名称允许在组内折行。

验证：相关 ESLint、Production build、git diff --check 通过。电脑操控发行版原窄侧栏中，标题一行、操作另起一行，运行、保存、关闭完整可读，禁用行为保留。证据 output/user-audit-20260907/evidence/fix-F09b-toolbar.png/.txt；构建日志 output/user-audit-20260907/fix-F09b-build.log。

尝试拖动扩大编辑区实际选中了文本，未形成宽度变化；fix-F09b-wide 不作为宽窗口通过证据。本提交只处理顶部工具栏，代码区语言设置的窄布局仍需后续统一布局检查。
