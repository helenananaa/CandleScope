# F17b 内置指标参数直接显示技术字段或英文标签

复现：生产构建→指标→已添加，MA、MACD、RSI 参数显示 Period、Source、Fast Period、Histogram Up Color 等英文，来源下拉显示 close/open。

修复：显示层为内置指标的已有参数键提供本地化映射，覆盖周期、价格来源、快慢/信号线周期、标准差倍数和各类颜色。MACD 按实际正负值语义显示“正值柱颜色/负值柱颜色”。参数名保留在标签的悬浮提示内。价格来源显示当地语言的价格名称或明确算式，下拉 value、参数键、参数值与计算逻辑不变。自定义指标继续使用作者标签，未知字段保留原文。九种语言补齐文案。

验证：

- 单元测试通过，覆盖内置参数翻译、自定义/未知字段回退、价格来源及加权收盘算式、语言切换。
- TypeScript、相关 ESLint、生产构建、git diff --check 通过。日志 `output/user-audit-20260907/F17b-*.log`。
- 电脑操控生产预览验证中文 MA/MACD/RSI 参数及来源下拉。将 MA 来源从收盘价改为开盘价后可重算，随后恢复收盘价；通过键盘将周期 20→21，主图更新 MA(21)，再恢复 20。
- 截图与 AX：`output/user-audit-20260907/evidence/fix-F17b-before`、`fix-F17b-localized`、`fix-F17b-source-open`、`fix-F17b-period-21`、`fix-F17b-restored`。

测试过程备注：电脑操控 set_value 对数值 stepper 曾留下空草稿，未提交新周期；改用点击、全选、键盘输入后成功完成 21→20 往返。此工具输入行为不据此判为产品缺陷。

边界：GUI 验证中文及上述三个指标；其他语言和其他内置参数映射通过类型/单元检查，未逐项 GUI 操作。未验证指标数值公式正确性。F17 的指标名称、market-data 等其他术语及图标风格仍单独待处理。F16 导出参数字符串暂仍保留技术字段，不包含在本次指标编辑面板修复中。
