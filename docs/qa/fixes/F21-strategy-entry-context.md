# F21 两个策略入口用途和数据上下文不清

修复：独立 strategy.html 入口改名“高级研究”，悬浮提示说明新标签页、需另选数据、不自动继承当前交易对和周期；当前图格入口改名“图表策略”，提示使用当前图表的交易对、周期和数据。九种语言同步。没有修改跳转地址、数据传递或策略计算逻辑。

验证：TypeScript、ESLint、生产构建、git diff --check 通过。电脑操控生产版确认两个名称可见及可访问提示完整；点击图表策略打开原测试器，显示“当前图表 · BTC-USDT-SWAP · 15m”；点击高级研究新开 strategy.html，显示“尚未选择数据”“此工作区未绑定实时图表”，与提示一致，随后关闭新标签页返回主图。证据 `output/user-audit-20260907/evidence/fix-F21-entries`、`fix-F21-chart-tester`、`fix-F21-independent`；日志 `F21-*.log`。

边界：本次修复入口语义，未新增上下文跨页传递，也未重新执行策略回测。GUI 验证中文桌面窗口，其他语言通过类型/构建检查。纯命名及提示修改未新增镜像单元测试。
