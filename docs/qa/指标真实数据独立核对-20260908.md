# 七个内置指标的真实数据独立核对

日期：2026-09-08；代码 73f52e6d。

从本机行情后端取得 OKX BTC-USDT-SWAP 15m 历史，选取 1498 根连续已收盘 K 线。响应报告尾部另缺 2 根，因此本核对不声称数据覆盖当前时刻；对实际取得的连续窗口逐点比较。

核对 MA20、EMA20、RSI14、MACD12/26/9、BOLL20×2、ATR14、VOL，共 11 条输出、16265 个非空值，全部匹配。绝对容差 1e-5，最大实际误差 2.8738577384501696e-7；各输出误差见结果 JSON。

参考算法独立于指标类状态：EMA/RMA 采用初始平均加指数权重展开，MA 使用窗口均值，布林带使用偏离均值的平方平均，RSI 使用涨跌平滑比率，真实波幅显式考虑前收盘缺口。还逐点核对预热空值掩码。

此处验证软件自身公开参数与计算约定，MACD 柱值沿用 2*(DIF-DEA)，EMA 使用简单平均初始化；不是把不同平台默认初始化/柱值倍率直接当成相同标准。也不证明每个参数组合、跨交易所流转、纠正数据或市场切换瞬态全部正确。

证据位于 output/user-audit-20260908/：indicator-reference-bars.json、check_indicator_reference.py、indicator-reference-result.json、indicator-reference-check.log。复跑：backend/.venv/bin/python output/user-audit-20260908/check_indicator_reference.py。
